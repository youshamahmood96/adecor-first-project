import React from 'react';
import ScrollView from './ScrollView'
const Mobile = () => {
    return (
       <React.Fragment>
       <ScrollView></ScrollView>
       </React.Fragment>
    );
};

export default Mobile;